conda_forge_output_validation: true
provider: {linux_ppc64le: azure, linux_aarch64: azure}
# remove the `azure` section and re-render before merging
azure:
  store_build_artifacts: False
github:
  branch_name: main
  tooling_branch_name: main
